#!/bin/bash -eu

case "$1" in
  -v|--version)
    version="$2"
esac

# Copy the version specific files to perl directory
shopt -s dotglob
touch $OPENSHIFT_HOMEDIR/a
cp -r $OPENSHIFT_PERL_DIR/versions/$version/etc/* $OPENSHIFT_PERL_DIR/etc
mkdir -p $OPENSHIFT_HOMEDIR/.cpanm
